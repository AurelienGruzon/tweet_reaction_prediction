# tweet_reaction_prediction

## Reproducibility

Raw data is not included in the repository.
Please download the Sentiment140 dataset and place it in:
data/raw/

Then run:
python -m src.data.make_dataset
python -m src.models.train_baseline

